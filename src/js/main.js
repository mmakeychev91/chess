// Libs
import 'focus-visible'

// Utils
import { fixFullHeight } from './utils/fix-fullheight'

// Components

document.addEventListener('DOMContentLoaded', function () {
  fixFullHeight()
})

// For Backend
// window.GraphModal = new GraphModal()
